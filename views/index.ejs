<html>
  <head>
    <title>Homepage</title>
  </head>
  <body>
    <%- include('navbar', {links: links}) %>
    <ul>
      <% messages.forEach((message,index) => { %> <%- include('message',
      {message: message}); %>
      <a href="/message/<%= index %>">
        <button>View Details</button>
      </a>
      <% }); %>
    </ul>
  </body>
</html>
