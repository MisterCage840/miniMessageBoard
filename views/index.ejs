<html>
  <head>
    <title>Homepage</title>
  </head>
  <body>
    <%- include('navbar', {links: links}) %>
    <ul>
      <% messages.forEach((message) => { %> <%- include('message', {message:
      message}); %> <% }); %>
    </ul>
  </body>
</html>
