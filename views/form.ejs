<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Add a Message · Mini Message Board</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <div class="page">
      <div class="shell form-card">
        <%- include('navbar', { links: links }) %>

        <header class="page-header">
          <div>
            <h1 class="page-title">Add a message</h1>
            <p class="page-subtitle">
              Share a quick thought with a name and a short message.
            </p>
          </div>
        </header>

        <main>
          <form action="/new" method="POST">
            <div class="form-field">
              <label for="user">Name</label>
              <input
                id="user"
                name="user"
                type="text"
                placeholder="Your name"
                required
              />
            </div>

            <div class="form-field">
              <label for="messageTxt">Message</label>
              <textarea
                id="messageTxt"
                name="messageTxt"
                rows="3"
                placeholder="What would you like to say?"
                required
              ></textarea>
            </div>

            <div class="form-footer">
              <a href="/" class="btn-secondary">Cancel</a>
              <button type="submit" class="btn-primary">Post message</button>
            </div>
          </form>
        </main>
      </div>
    </div>
  </body>
</html>
